# Dependencies
/node_modules
/vendor

# Environment & secrets
.env
.env.*
!.env.example

# Build output — DI-TRACK agar `git pull` di server (tanpa Node) memperbarui tampilan.
# Build di Mac (npm run build) lalu commit folder public/build.
/public/hot

# Laravel runtime
/storage/*.key
/storage/framework
/storage/logs
/bootstrap/cache/*.php

# Editor & OS
.DS_Store
.idea
.vscode
*.swp

# Backups & file uji sementara
*.backup-*
.htaccess.backup-*
_*.php
composer.phar
composer-setup.php
